node_modules/
.next/
out/
dist/
.test-build/
*.tsbuildinfo
*.env
*.env.local
.env.*
!.env.example
__pycache__/
*.pyc
*.pyo
ml-service/models/*.pkl
.DS_Store
# Root-only: the Vercel root-directory build output (scripts/build-root-placeholder.mjs).
# Anchored with a leading slash so it does NOT swallow a workspace's own public/
# directory, e.g. a Next.js frontend/public/ static folder.
/public/
*.tsbuildinfo
packages/*/dist

# Local archive/staging dir (contains a 210MB Archive.zip) - never commit
quanntflow/
*.zip
!qf-firecrawl (1).zip
!qf-flow-engine.zip
!quantflow.zip
