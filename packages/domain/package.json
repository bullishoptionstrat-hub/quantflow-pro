{
  "name": "@quantflow/domain",
  "version": "1.0.0",
  "description": "Canonical shared contracts: DataResult, Provenance, Quality, RuntimeMode, point-in-time.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc -p tsconfig.all.json",
    "test": "tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.22.4",
    "typescript": "^5.4.5"
  }
}
